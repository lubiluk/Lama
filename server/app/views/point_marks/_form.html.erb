<%= form_for(@point_mark) do |f| %>
  <% if @point_mark.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@point_mark.errors.count, "error") %> prohibited this point_mark from being saved:</h2>

      <ul>
      <% @point_mark.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
