<%= form_for(@geometry_mark) do |f| %>
  <% if @geometry_mark.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@geometry_mark.errors.count, "error") %> prohibited this geometry_mark from being saved:</h2>

      <ul>
      <% @geometry_mark.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :radius %><br />
    <%= f.text_field :radius %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
